name: Print
on: push

jobs:
  print:
    runs-on: ubuntu-latest
    env:
      APP_NAME: ${{ vars.APP_NAME }}
    steps:
      - name: Create text file
        run: |
          echo "App Name: $APP_NAME" > info.txt
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-info
          path: info.txt
        




